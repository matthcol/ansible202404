[Unit]
Description=A Spring Boot application
After=syslog.target network.target

[Service]
User={{ user_api }}
WorkingDirectory={{ var_directory}}
ExecStart=java -jar {{ var_artifact }}
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target
